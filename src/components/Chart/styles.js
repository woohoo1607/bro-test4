import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  chartContainer: {
    marginTop: "20px",
  },
  generateBtnContainer: {
    textAlign: "right",
    paddingRight: "40px",
  },
}));
